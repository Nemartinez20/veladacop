<header class="fixed top-0 w-full z-50 text-white">
  <nav
    class="flex items-center px-6 py-4 text-sm font-light justify-between max-w-6xl mx-auto"
  >
    <div class="flex flex-row gap-8 justify-start">
      <a href="/la-porra" class="inline-block hover:underline hover:scale-110"
        >HAZ TU <strong>PORRA</strong></a
      >

      <a href="/combate" class="inline-block hover:underline hover:scale-110"
        >VER <strong>COMBATES</strong></a
      >
    </div>
    <div class="hidden md:flex flex-col items-end">
      <span class="inline-block opacity-90 relative cursor-not-allowed">
        COMPRA LAS <strong>ENTRADAS</strong>
        <span
          class="absolute top-4 inset-0 tetx-[10px] text-yellow-500 mt-1 text-center"
        >
          Próximamente
        </span>
      </span>
    </div>
  </nav>
</header>

<style>
  header {
    /* border: 5px solid rebeccapurple; */
    background: transparent;
    border-bottom: 1px solid transparent;
    animation-name: headerblur;
    animation-duration: 0.3s;
    animation-timing-function: linear;
    animation-fill-mode: both; /* Se mantiene el primer y último estado */
    animation-timeline: scroll(); /* que sea al hacer el scroll*/
    animation-range: 0 250px; /* desde donde epieza en x y donde empieza en y*/
  }

  @keyframes headerblur {
    0% {
      backdrop-filter: blur(0px);
      background: transparent;
    }
    100% {
      backdrop-filter: blur(10px);
      background: rgba(0, 0, 0, 0.2);
      padding-bottom: var(--spacing-6);
      border-bottom-color: black;
    }
  }
</style>
